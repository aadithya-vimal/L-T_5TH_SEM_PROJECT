const router=require('express').Router();const c=require('../controllers/notificationController');const asyncHandler=require('../utils/asyncHandler');const auth=require('../middleware/auth');const v=require('../validators');const validate=require('../middleware/validate');router.use(auth);router.get('/',asyncHandler(c.list));router.patch('/read-all',asyncHandler(c.readAll));router.patch('/:id/read',v.resourceId,validate,asyncHandler(c.read));module.exports=router;
