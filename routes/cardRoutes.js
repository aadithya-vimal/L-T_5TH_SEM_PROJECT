const router=require('express').Router();const c=require('../controllers/cardController');const asyncHandler=require('../utils/asyncHandler');const auth=require('../middleware/auth');const v=require('../validators');const validate=require('../middleware/validate');router.use(auth);router.get('/',asyncHandler(c.list));router.get('/:id',v.resourceId,validate,asyncHandler(c.get));router.post('/',v.cardIssue,validate,asyncHandler(c.issue));router.patch('/:id/block',v.resourceId,validate,asyncHandler(c.block));router.patch('/:id/unblock',v.resourceId,validate,asyncHandler(c.unblock));module.exports=router;
