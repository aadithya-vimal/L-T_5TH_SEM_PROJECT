const router=require('express').Router();const c=require('../controllers/adminController');const asyncHandler=require('../utils/asyncHandler');const auth=require('../middleware/auth');const role=require('../middleware/authorize');router.use(auth,role('BANK_ADMIN','BANK_STAFF'));router.get('/users',asyncHandler(c.users));router.patch('/users/:id/status',role('BANK_ADMIN'),asyncHandler(c.userStatus));router.get('/accounts',asyncHandler(c.accounts));router.get('/transactions',asyncHandler(c.transactions));router.get('/reports',asyncHandler(c.reports));router.get('/analytics',asyncHandler(c.analytics));module.exports=router;
