const router=require('express').Router();const c=require('../controllers/beneficiaryController');const asyncHandler=require('../utils/asyncHandler');const v=require('../validators');const auth=require('../middleware/auth');router.use(auth);router.post('/',v.beneficiaryCreate,require('../middleware/validate'),asyncHandler(c.create));router.get('/',asyncHandler(c.list));router.get('/:id',v.beneficiaryId,require('../middleware/validate'),asyncHandler(c.get));router.put('/:id',v.beneficiaryId,require('../middleware/validate'),asyncHandler(c.update));router.delete('/:id',v.beneficiaryId,require('../middleware/validate'),asyncHandler(c.remove));module.exports=router;
